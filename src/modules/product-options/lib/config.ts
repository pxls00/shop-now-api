import type { IConfigModule } from './config.types'

const config: IConfigModule = {
  moduleRouteBrandURL: '/product_brands',
  moduleRouteBrandItemIdURL: 'brand_id',
  moduleRouteOptionURL: '/product_options',
  moduleRouteOptionItemIdURL: 'option_id',
}

export default config
