import type { IConfigModule } from './config.types'

const config: IConfigModule = {
  moduleRouteCompanySearchURL: '/company_search',
  moduleRouteProductSearchURL: '/product_search',
}

export default config
