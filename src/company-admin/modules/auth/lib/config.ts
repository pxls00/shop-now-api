import type { IConfigModule } from './config.types'

const config: IConfigModule = {
  moduleRouteBaseURL: '/auth',
  moduleRegisterRoute: '/register',
  moduleLoginRoute: '/login',
  moduleLogoutRoute: '/logout',
}

export default config
